# Mẫu biến môi trường cho hạ tầng dev. Copy thành deploy/.env và điền giá trị thật.
# KHÔNG commit deploy/.env (đã bị .gitignore). File .env.example này an toàn để commit.

# Mật khẩu Postgres cho compose dev (mặc định "socialapp" nếu bỏ trống).
POSTGRES_PASSWORD=socialapp

# --- Các biến sẽ dùng từ GĐ1+ (chưa cần ở GĐ0) ---
# JWT_SIGNING_KEY=thay-bang-khoa-bi-mat-du-dai->=32-ky-tu
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=socialapp-dev
